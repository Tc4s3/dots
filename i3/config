###############################################
#░▒█▀▄▀█░█▀▀▄░█▀▀█░█▀▄▀█░█▀▀█░█▀▀░█░░█░█▀▀▄░█▀▀█#
#░▒█▒█▒█░█░▒█░░▒▀▄░█░▀░█░█▄▀█░▀▀▄░█▄▄█░█░▒█░░▒▀▄#
#░▒█░░▒█░▀░░▀░█▄▄█░▀░░▒▀░█▄▄█░▄▄▀░▄▄▄▀░▀░░▀░█▄▄█#
#################################################

# Set Mod key
set $mod Mod4
set $mod1 Mod1
# Custom sets
set $bar /home/operator/Scripts/polyboot.sh
# Notifications by Dunst
exec_always --no-startup-id dunst
    # Reboot Dunst
    bindsym $mod+d exec ~/Scripts/rbdunst.sh
# Input remapper
exec --no-startup-id input-remapper-gtk --load-config /home/operator/.config/input-remapper-2/config.json
# Compositor
exec --no-startup-id picom -b
# Fonts
# System wide
font pango: Terminess Nerd Font Bold 16
# Title bars
font pango: Terminess Nerd Font Bold 14
# wallpaper
exec_always feh --bg-fill /home/operator/Pictures/namibia2kpxlD.png
# Autostart programs
exec --no-startup-id lxterminal
exec --no-startup-id mullvad-vpn
exec --no-startup-id bitwarden-desktop
exec --no-startup-id openrgb
exec --no-startup-id tilda -g ~/.config/tilda/config_1 -h
# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3
# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1% && $refresh_i3status
    # Toggle audio output
    bindsym $mod+o exec ~/Scripts/toggleaudio.sh
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +5%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-
# Colors                 
    #item                   #border #backg  #text   #indi   #chbord
    client.focused          #B5A178 #E8CB74 #563123 #B5A178 #B5A178
    client.focused_inactive #E8CB74 #EDD796 #B5A178 #E8CB74 #E8CB74
    client.unfocused        #E8CB74 #EDD796 #B5A178 #E8CB74 #E8CB74
    client.urgent           #52595C #7E94A3 #220F08 #52595C #52595C                                                                   
default_border pixel 6
default_border normal
for_window [class="firefox"] border normal 4
for_window [floating] border normal
for_window [class="Tilda"] border pixel 0

# Gaps
gaps outer 30
# Mouse organising
tiling_drag modifier titlebar
floating_modifier $mod
# Terminal
bindsym $mod+t exec lxterminal
# Close
bindsym $mod+x kill
# Rofi
bindsym $mod+space exec rofi -show drun
# Tab between active workspaces
bindsym $mod+Tab workspace back_and_forth
bindsym Control+$mod+period workspace next
# Change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
# Move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
# Split H
bindsym $mod+b split h
# |Split V
bindsym $mod+v split v
# Fullscreen
bindsym $mod+Shift+f fullscreen toggle
# Float
bindsym $mod+f floating toggle
    # Forced floating
    for_window [class="Bitwarden"] floating enable, resize set 1065 840
    for_window [class="Gpick"] floating enable, resize set 800 600
    for_window [class="Pcmanfm"] floating enable, resize set 1123 860
    for_window [class="pavucontrol"] floating enable, resize set 780 538
    for_window [class="ark"] floating enable, resize set 743 585
    for_window [class="SyncTERM"] floating enable resize set 1303 977
    # Float focus
    bindsym $mod+Shift+space focus mode_toggle
# focus the parent container
bindsym $mod+a focus parent
# Resize
bindsym $mod+r mode "resize"
mode "resize" {
        bindsym h resize shrink width 1 px or 1 ppt
        bindsym j resize grow height 1 px or 1 ppt
        bindsym k resize shrink height 1 px or 1 ppt
        bindsym l resize grow width 1 px or 1 ppt
        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
# Workspaces
set $ws1 "〇"
set $ws2 "一"
set $ws3 "二"
set $ws4 "三"
set $ws5 "四"
set $ws6 "五"
set $ws7 "六"
set $ws8 "七"
set $ws9 "八"
set $ws0 "九"
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws0
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws0
#monitors
exec --no-startup-id xrandr --output eDP-1 --primary --mode 2560x1600 --rate 60.01 --output HDMI-1-0 --mode 1920x1080 --rate 120.00 --rotate left
    # variables
        set $em HDMI-1-0 
		set $lm eDP-1
    # workspaces
	    workspace 〇 output $lm			
        workspace 一 output $lm
        workspace 二 output $lm
        workspace 三 output $lm
        workspace 四 output $lm
        workspace 五 output $em
        workspace 六 output $em
        workspace 七 output $em
        workspace 八 output $em
        workspace 九 output $em
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# Custom keys
	# applications
	bindsym $mod+p exec bitwarden-desktop
	bindsym $mod+i exec mullvad-vpn
	bindsym $mod+g exec firefox
    bindsym $mod+s exec spectacle
    bindsym $mod+e exec pcmanfm
    bindsym $mod+n exec ~/Scripts/fuzpad.sh
    bindsym $mod+c exec ~/Scripts/roficalc.sh
	# session management
	bindsym Control+$mod+l exec "i3lock -i /home/operator/Pictures/namibia2kpxlD.png -t"
    bindsym Control+$mod+s exec "systemctl suspend && i3lock -i i3lock -i /home/operator/Pictures/namibia2kpxlD.png -t"
    bindsym Control+$mod+q exec "i3-nagbar -t warning -m 'Exit i3?' -B 'Yes' 'i3-msg exit'"
    # Monitor switching
        # Dual Screen Mode
        bindsym $mod+Shift+o exec ~/Documents/arandr.sh
        # Vertical Mode
        bindsym $mod+Shift+p exec ~/Documents/arandrp.sh    
        # Laptop Mode
        bindsym $mod+Shift+i exec ~/Documents/arandrl.sh
# Polybar
exec_always --no-startup-id polybar main
	# reload pb config
	bindsym $mod+Shift+b exec $bar
# Disable dpms
exec --no-startup-id xset -dpms
exec --no-startup-id xset s off
exec --no-startup-id xset s noblank

